# Changesets

This directory is used by [Changesets](https://github.com/changesets/changesets) to track version bumps.

To create a new changeset, run:

```bash
pnpm changeset
```

Then follow the prompts to describe your change and select affected packages.
